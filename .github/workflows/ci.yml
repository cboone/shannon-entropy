name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  lean:
    name: Build, lint, and test (Lean)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: leanprover/lean-action@v1

      - name: Lean lint
        run: lake lint

      - name: Lean test
        run: lake test

  markdown:
    name: Lint (Markdown and spelling)
    uses: cboone/gh-actions/.github/workflows/text-lint.yml@2bbea614b90f8b96e991f630276893fcf67aa329 # v2.1.3
    with:
      run-cspell: true
      run-prettier: false
